from django.db import models

# The dashboard app is a presentation layer; it has no models of its own.
# Data models (Asset, AssetStatus, Category) live in config/models.py
# and are shared across the project.
